# 后端接口对接说明

## 概述

本文档详细说明了前端代码如何与后端点赞收藏接口进行对接，确保数据格式的一致性和功能的完整性。

## 统一响应格式

后端所有接口都使用统一的响应格式：

```typescript
interface ApiResponse<T = any> {
  code: number      // 状态码：200成功，500失败
  message: string   // 响应消息
  data: T          // 响应数据
}
```

## 前端适配说明

### 1. API接口层适配

所有API接口都已更新为支持后端统一响应格式：

```typescript
// 更新前
export function getLikeFavoriteStatus(experimentId: number, userId: number) {
  return request.get<LikeStatus>(`/like-favorite/experiment/${experimentId}`, {
    params: { userId }
  })
}

// 更新后
export function getLikeFavoriteStatus(experimentId: number, userId: number) {
  return request.get<ApiResponse<LikeStatus>>(`/like-favorite/experiment/${experimentId}`, {
    params: { userId }
  })
}
```

### 2. 数据获取适配

在组件中获取数据时，需要正确处理响应格式：

```typescript
// 更新前
const response = await likeFavoriteApi.getLikeFavoriteStatus(experimentId, userId)
const data = response.data || response

// 更新后
const response = await likeFavoriteApi.getLikeFavoriteStatus(experimentId, userId)
const data = response.data?.data || response.data
```

### 3. 状态管理适配

在Pinia store中，所有API调用都已适配新的响应格式：

```typescript
// 获取用户点赞收藏状态
const statusResponse = await likeFavoriteApi.getLikeFavoriteStatus(experimentId, userStore.user.id)
const statusData = statusResponse.data?.data || statusResponse.data

// 获取点赞和收藏数量
const likeCountResponse = await likeFavoriteApi.getResourceLikeCount(experimentId)
const favoriteCountResponse = await likeFavoriteApi.getResourceFavoriteCount(experimentId)

const likeCount = Number(likeCountResponse.data?.data || likeCountResponse.data) || 0
const favoriteCount = Number(favoriteCountResponse.data?.data || favoriteCountResponse.data) || 0
```

## 接口对接详情

### 1. 点赞功能接口

#### 查询点赞/收藏状态
```typescript
// 前端调用
const response = await likeFavoriteApi.getLikeFavoriteStatus(experimentId, userId)
const status = response.data?.data || response.data
// status: { liked: boolean, favorited: boolean }
```

#### 点赞资源
```typescript
// 前端调用
await likeFavoriteApi.likeResource(resourceId, userId)
// 后端响应: { code: 200, message: "点赞成功", data: null }
```

#### 取消点赞
```typescript
// 前端调用
await likeFavoriteApi.unlikeResource(resourceId, userId)
// 后端响应: { code: 200, message: "取消点赞成功", data: null }
```

### 2. 收藏功能接口

#### 添加收藏
```typescript
// 前端调用
const response = await likeFavoriteApi.addFavorite({
  userId: userStore.user.id,
  resourceId: experimentId
})
const favoriteId = response.data?.data || response.data
// favoriteId: number (收藏记录ID)
```

#### 移除收藏
```typescript
// 前端调用
await likeFavoriteApi.removeFavorite(favoriteId)
// 后端响应: { code: 200, message: "移除收藏成功", data: 1 }
```

### 3. 查询功能接口

#### 获取资源点赞数量
```typescript
// 前端调用
const response = await likeFavoriteApi.getResourceLikeCount(resourceId)
const count = Number(response.data?.data || response.data) || 0
```

#### 获取资源收藏数量
```typescript
// 前端调用
const response = await likeFavoriteApi.getResourceFavoriteCount(resourceId)
const count = Number(response.data?.data || response.data) || 0
```

### 4. 管理员功能接口

#### 获取用户统计
```typescript
// 前端调用
const response = await likeFavoriteApi.getAllUsersLikeFavoriteStats()
const data = response.data?.data || response.data
// data: {
//   userStats: Array<{userId, username, likeCount, favoriteCount, lastActivity}>,
//   totalLikes: number,
//   totalFavorites: number,
//   totalUsers: number,
//   totalResources: number
// }
```

#### 获取点赞记录（分页）
```typescript
// 前端调用
const response = await likeFavoriteApi.getAllLikes(page, size)
const data = response.data?.data || response.data
// data: {
//   records: Array<{id, userId, resourceId, createTime, username, resourceName}>,
//   total: number,
//   current: number,
//   size: number
// }
```

## 错误处理

### 1. 统一错误处理

前端统一处理后端返回的错误：

```typescript
try {
  const response = await likeFavoriteApi.likeResource(resourceId, userId)
  // 检查响应状态
  if (response.data?.code === 200) {
    // 操作成功
    ElMessage.success('操作成功')
  } else {
    // 操作失败
    ElMessage.error(response.data?.message || '操作失败')
  }
} catch (error) {
  // 网络错误或其他异常
  ElMessage.error('网络错误，请稍后重试')
}
```

### 2. 常见错误码处理

```typescript
// 后端错误码
const ERROR_CODES = {
  SUCCESS: 200,
  FAILED: 500
}

// 常见错误消息
const ERROR_MESSAGES = {
  ALREADY_LIKED: '您已经点赞过该资源',
  NOT_LIKED: '您还没有点赞该资源',
  ALREADY_FAVORITED: '您已经收藏过该资源',
  NOT_FAVORITED: '您还没有收藏该资源',
  NETWORK_ERROR: '网络错误，请稍后重试'
}
```

## 数据格式转换

### 1. 时间格式处理

后端返回的时间格式为ISO字符串，前端需要格式化显示：

```typescript
// 格式化时间
const formatTime = (time: string) => {
  if (!time) return ''
  return new Date(time).toLocaleString('zh-CN')
}
```

### 2. 数字格式处理

确保数字类型正确转换：

```typescript
// 安全转换数字
const safeNumber = (value: any, defaultValue: number = 0) => {
  const num = Number(value)
  return isNaN(num) ? defaultValue : num
}
```

## 权限验证

### 1. 用户登录状态检查

所有需要用户身份的操作都需要检查登录状态：

```typescript
// 检查用户登录状态
if (!userStore.user?.id) {
  ElMessage.warning('请先登录')
  return
}
```

### 2. 管理员权限检查

管理员功能需要检查权限：

```typescript
// 检查管理员权限
const isAdmin = computed(() => userStore.userRole === 'SYSTEM_ADMIN')
if (!isAdmin.value) {
  ElMessage.error('权限不足')
  return
}
```

## 性能优化

### 1. 缓存机制

使用Pinia store缓存数据，避免重复请求：

```typescript
// 缓存实验状态
const experimentStates = ref<ExperimentLikeFavorite>({})

// 获取状态时先检查缓存
const getExperimentState = computed(() => {
  return (experimentId: number): LikeFavoriteState => {
    return experimentStates.value[experimentId] || {
      liked: false,
      favorited: false,
      likeCount: 0,
      favoriteCount: 0
    }
  }
})
```

### 2. 批量操作

支持批量获取多个实验的状态：

```typescript
// 批量获取状态
const fetchMultipleExperimentStates = async (experimentIds: number[]) => {
  if (!userStore.user?.id) return
  
  const promises = experimentIds.map(id => fetchExperimentState(id))
  await Promise.allSettled(promises)
}
```

## 测试建议

### 1. 接口测试

使用curl或Postman测试后端接口：

```bash
# 测试点赞功能
curl -X POST "http://localhost:8080/like-favorite/like/1?userId=1"

# 测试查询状态
curl -X GET "http://localhost:8080/like-favorite/experiment/1?userId=1"
```

### 2. 前端测试

在浏览器控制台测试前端功能：

```javascript
// 测试状态管理
const store = useLikeFavoriteStore()
await store.fetchExperimentState(1)
console.log(store.getExperimentState(1))

// 测试点赞操作
await store.toggleLike(1)
```

## 部署注意事项

### 1. 环境配置

确保前端请求的API地址与后端服务地址一致：

```typescript
// 开发环境
const API_BASE_URL = 'http://localhost:8080'

// 生产环境
const API_BASE_URL = 'https://your-domain.com'
```

### 2. 跨域配置

后端需要配置CORS支持前端跨域请求：

```java
@CrossOrigin(origins = "*", maxAge = 3600)
@RestController
public class LikeFavoriteController {
    // 控制器代码
}
```

### 3. 错误监控

建议添加错误监控和日志记录：

```typescript
// 全局错误处理
window.addEventListener('unhandledrejection', (event) => {
  console.error('未处理的Promise拒绝:', event.reason)
  // 发送错误报告
})
```

## 总结

通过以上适配，前端代码已完全兼容后端接口的统一响应格式。主要改进包括：

1. ✅ 统一响应格式处理
2. ✅ 完善的错误处理机制
3. ✅ 类型安全的API调用
4. ✅ 性能优化的缓存机制
5. ✅ 权限验证和安全性
6. ✅ 完整的测试和部署指南

现在前端可以无缝对接后端点赞收藏功能，提供完整的用户体验。 